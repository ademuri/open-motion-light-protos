syntax = "proto3";

import "nanopb.proto";

message SerialRequest { bool request_config = 1; }

message SerialResponse { StatusPb status = 1; }

message StatusPb {
  string firmware_version = 1 [ (nanopb).max_size = 20 ];
  uint32 battery_voltage_millivolts = 2;
};
